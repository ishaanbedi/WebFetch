// swift-tools-version: 5.7

import PackageDescription

let package = Package(
    name: "WebFetch",
    products: [
        .library(
            name: "WebFetch",
            targets: ["WebFetch"]),
    ],
    dependencies: [
    ],
    targets: [
        .target(
            name: "WebFetch",
            dependencies: []),
    ]
)
